# 物品管理 Phase 0：场地布置入库 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把公会场地布置从浏览器 localStorage 迁移到 Supabase，成为一个全团队共享、可被物品台账引用的服务端布局。

**Architecture:** 新增 `venues / venue_floors / venue_items` 三张表（用 text 主键，直接沿用画布现有的字符串 id，避免在客户端做 id 重映射）。提供 `GET /api/venue`（返回嵌套布局）和 `PUT /api/venue`（整份 upsert + 删除已移除的楼层/对象）。把行 ↔ 嵌套布局的转换抽成纯函数 `layout-sync.ts` 以便用 `node:test` 单测；服务层只做薄薄的数据库读写。场地页改为从 API 加载、防抖自动保存；localStorage 降级为离线兜底 + 一次性导入来源。

**Tech Stack:** Next.js App Router、Supabase (`@supabase/supabase-js`, service-role client)、TypeScript、`node --test --experimental-strip-types`（纯函数单测）。

---

## 文件结构

**新增：**
- `supabase/migrations/029_venue.sql` — 三张表 + 索引 + updated_at 触发器 + 预置共享场地数据。
- `src/lib/venue/layout-sync.ts` — 纯函数：行数组 ↔ `VenueLayout` 互转（`rowsToLayout` / `layoutToRows`）。
- `src/lib/venue/layout-sync.test.ts` — 上述纯函数的单测。
- `src/lib/venue/service.ts` — `getVenueLayout()` / `saveVenueLayout()`，返回 `ServiceResult`。
- `src/app/api/venue/route.ts` — `GET` / `PUT`。

**修改：**
- `src/app/[locale]/(app)/guild-venue/page.tsx` — 加载与保存改走 API；localStorage 一次性导入。
- `package.json` — 把新测试文件加入 `test` 脚本。
- `messages/zh.json` / `messages/en.json` — 加载/保存/导入相关文案。

**复用既有约定：**
- `ServiceResult<T>` / `ServiceError` / `httpStatusForError`（与 `src/lib/expenses/service.ts` 同形，复制一份到 venue 服务，保持各模块自洽——项目现状就是每个 service 各自声明）。
- `createServerClient()`（`src/lib/supabase/server.ts`，service-role）。
- `authGuard()`（`src/lib/auth/guard.ts`，返回 `user` 或 `NextResponse`）。
- `update_updated_at()` 触发器函数（`supabase/migrations/001_initial_schema.sql:119` 已定义）。

---

## Task 1：数据库迁移 `029_venue.sql`

**Files:**
- Create: `supabase/migrations/029_venue.sql`

- [ ] **Step 1: 写迁移文件**

```sql
-- ============================================================
-- Migration 029: Venue persistence
-- 把公会场地布局从浏览器 localStorage 迁入数据库，使其可全团队
-- 共享，并可被物品台账（迁移 030）引用。
-- 主键使用 text，直接沿用画布现有的字符串 id。
-- ============================================================

create table venues (
  id          text         primary key,
  name        text         not null,
  width       integer      not null,
  height      integer      not null,
  created_at  timestamptz  not null default now(),
  updated_at  timestamptz  not null default now()
);

create table venue_floors (
  id               text         primary key,
  venue_id         text         not null references venues(id) on delete cascade,
  name             text         not null,
  width            integer      not null,
  height           integer      not null,
  background_image text,
  sort_order       integer      not null default 0,
  created_at       timestamptz  not null default now(),
  updated_at       timestamptz  not null default now()
);

create table venue_items (
  id          text         primary key,
  floor_id    text         not null references venue_floors(id) on delete cascade,
  type        text         not null,
  name        text         not null default '',
  x           integer      not null default 0,
  y           integer      not null default 0,
  width       integer      not null default 0,
  height      integer      not null default 0,
  rotation    numeric      not null default 0,
  status      text         not null default 'planned',
  note        text         not null default '',
  z_index     integer      not null default 0,
  created_at  timestamptz  not null default now(),
  updated_at  timestamptz  not null default now(),

  constraint venue_items_type_valid check (type in
    ('equipment','renovation','area','corridor',
     'door_inward','door_sliding','fire','power','network')),
  constraint venue_items_status_valid check (status in
    ('planned','in_progress','completed','maintenance'))
);

create index idx_venue_floors_venue on venue_floors(venue_id, sort_order);
create index idx_venue_items_floor  on venue_items(floor_id, z_index);

create trigger venues_updated_at       before update on venues       for each row execute function update_updated_at();
create trigger venue_floors_updated_at before update on venue_floors for each row execute function update_updated_at();
create trigger venue_items_updated_at  before update on venue_items  for each row execute function update_updated_at();

-- 预置单一共享场地，镜像 DEFAULT_VENUE_LAYOUT。
insert into venues (id, name, width, height) values ('guild-main', '主场地', 1200, 800);

insert into venue_floors (id, venue_id, name, width, height, sort_order) values
  ('floor-1', 'guild-main', '1F', 1200, 800, 0),
  ('floor-2', 'guild-main', '2F', 1200, 800, 1);

insert into venue_items (id, floor_id, type, name, x, y, width, height, rotation, status, note, z_index) values
  ('eq-1',       'floor-1', 'equipment',   '直播设备架',     120,  80, 160,  80, 0, 'completed',   '靠墙放置，保留走线空间。',     0),
  ('area-1',     'floor-1', 'renovation',  '直播间 A 装修区', 360, 120, 260, 180, 3, 'in_progress', '吸音墙和灯光轨道施工中。',     1),
  ('corridor-1', 'floor-1', 'corridor',    '主通道',         120, 560, 620,  72, 0, 'planned',     '保持通道净宽，不堆放设备。',   2),
  ('door-1',     'floor-1', 'door_inward', '主入口',         700, 600,  32,  32, 0, 'completed',   '内开门，注意开门半径。',       3),
  ('fire-1',     'floor-1', 'fire',        '灭火器',         980, 280,  32,  32, 0, 'completed',   '消防点位需保持可见。',         4),
  ('power-1',    'floor-1', 'power',       '设备区电源',     200, 200,  32,  32, 0, 'planned',     '',                             5);
```

- [ ] **Step 2: 应用迁移并验证**

Run（按项目实际的迁移执行方式，例如 supabase CLI）：
```bash
supabase db push   # 或项目约定的迁移命令
```
Expected: 三张表创建成功；`select count(*) from venue_items;` 返回 `6`；`select id from venues;` 返回 `guild-main`。
若本地无法连库，则人工审查 SQL：确认外键 `on delete cascade`、两个 check 约束的取值集合与 `src/venue/layoutData.ts` 的 `VENUE_ITEM_TYPE_OPTIONS` / `VENUE_ITEM_STATUS_OPTIONS` 完全一致。

- [ ] **Step 3: 提交**

```bash
git add supabase/migrations/029_venue.sql
git commit -m "feat(venue): add venues/venue_floors/venue_items tables (migration 029)"
```

---

## Task 2：纯函数 `layout-sync.ts`（行 ↔ 布局）

**Files:**
- Create: `src/lib/venue/layout-sync.ts`
- Test: `src/lib/venue/layout-sync.test.ts`

- [ ] **Step 1: 先写失败测试**

`src/lib/venue/layout-sync.test.ts`：
```ts
import assert from 'node:assert/strict'
import test from 'node:test'

import { rowsToLayout, layoutToRows } from './layout-sync'
import type { VenueLayout } from '@/venue/layoutData'

const SAMPLE: VenueLayout = {
  venueId: 'guild-main',
  name: '主场地',
  width: 1200,
  height: 800,
  floors: [
    {
      id: 'floor-1',
      name: '1F',
      width: 1200,
      height: 800,
      backgroundImage: 'data:image/png;base64,xxx',
      items: [
        { id: 'eq-1', type: 'equipment', name: '设备', x: 1, y: 2, width: 3, height: 4, rotation: 0, status: 'completed', note: 'n' },
        { id: 'area-1', type: 'area', name: '空间', x: 5, y: 6, width: 7, height: 8, rotation: 3, status: 'planned', note: '' },
      ],
    },
    { id: 'floor-2', name: '2F', width: 1200, height: 800, items: [] },
  ],
}

test('layoutToRows flattens floors/items with sort_order + z_index + floor_id', () => {
  const { venue, floors, items } = layoutToRows(SAMPLE)
  assert.deepEqual(venue, { id: 'guild-main', name: '主场地', width: 1200, height: 800 })
  assert.equal(floors.length, 2)
  assert.deepEqual(
    floors.map((f) => [f.id, f.venue_id, f.sort_order, f.background_image]),
    [['floor-1', 'guild-main', 0, 'data:image/png;base64,xxx'], ['floor-2', 'guild-main', 1, null]],
  )
  assert.equal(items.length, 2)
  assert.deepEqual(
    items.map((i) => [i.id, i.floor_id, i.z_index]),
    [['eq-1', 'floor-1', 0], ['area-1', 'floor-1', 1]],
  )
})

test('rowsToLayout reassembles nested layout ordered by sort_order / z_index', () => {
  const { venue, floors, items } = layoutToRows(SAMPLE)
  // 打乱顺序，验证函数按 sort_order / z_index 重新排序
  const shuffledFloors = [floors[1], floors[0]]
  const shuffledItems = [items[1], items[0]]
  const rebuilt = rowsToLayout(venue, shuffledFloors, shuffledItems)
  assert.deepEqual(rebuilt, SAMPLE)
})

test('rowsToLayout drops background_image when null', () => {
  const rebuilt = rowsToLayout(
    { id: 'v', name: 'n', width: 10, height: 10 },
    [{ id: 'f1', venue_id: 'v', name: 'F', width: 10, height: 10, background_image: null, sort_order: 0 }],
    [],
  )
  assert.equal('backgroundImage' in rebuilt.floors[0], false)
})
```

- [ ] **Step 2: 运行测试确认失败**

Run:
```bash
node --test --experimental-strip-types src/lib/venue/layout-sync.test.ts
```
Expected: FAIL，报找不到模块 `./layout-sync` 或导出。

- [ ] **Step 3: 写实现**

`src/lib/venue/layout-sync.ts`：
```ts
import type { VenueItemType, VenueItemStatus, VenueLayout } from '@/venue/layoutData'

// 数据库行形态（text 主键，直接沿用画布的字符串 id）。
export interface VenueRow {
  id: string
  name: string
  width: number
  height: number
}

export interface VenueFloorRow {
  id: string
  venue_id: string
  name: string
  width: number
  height: number
  background_image: string | null
  sort_order: number
}

export interface VenueItemRow {
  id: string
  floor_id: string
  type: VenueItemType
  name: string
  x: number
  y: number
  width: number
  height: number
  rotation: number
  status: VenueItemStatus
  note: string
  z_index: number
}

// 嵌套布局 → 扁平行数组。sort_order / z_index 取数组下标，保留图层与楼层顺序。
export function layoutToRows(layout: VenueLayout): {
  venue: VenueRow
  floors: VenueFloorRow[]
  items: VenueItemRow[]
} {
  const venue: VenueRow = {
    id: layout.venueId,
    name: layout.name,
    width: layout.width,
    height: layout.height,
  }
  const floors: VenueFloorRow[] = layout.floors.map((floor, index) => ({
    id: floor.id,
    venue_id: layout.venueId,
    name: floor.name,
    width: floor.width,
    height: floor.height,
    background_image: floor.backgroundImage ?? null,
    sort_order: index,
  }))
  const items: VenueItemRow[] = layout.floors.flatMap((floor) =>
    floor.items.map((item, index) => ({
      id: item.id,
      floor_id: floor.id,
      type: item.type,
      name: item.name,
      x: item.x,
      y: item.y,
      width: item.width,
      height: item.height,
      rotation: item.rotation,
      status: item.status,
      note: item.note,
      z_index: index,
    })),
  )
  return { venue, floors, items }
}

// 扁平行数组 → 嵌套布局。按 sort_order / z_index 排序后组装。
export function rowsToLayout(
  venue: VenueRow,
  floors: VenueFloorRow[],
  items: VenueItemRow[],
): VenueLayout {
  const sortedFloors = [...floors].sort((a, b) => a.sort_order - b.sort_order)
  const sortedItems = [...items].sort((a, b) => a.z_index - b.z_index)

  return {
    venueId: venue.id,
    name: venue.name,
    width: venue.width,
    height: venue.height,
    floors: sortedFloors.map((floor) => ({
      id: floor.id,
      name: floor.name,
      width: floor.width,
      height: floor.height,
      ...(floor.background_image ? { backgroundImage: floor.background_image } : {}),
      items: sortedItems
        .filter((item) => item.floor_id === floor.id)
        .map((item) => ({
          id: item.id,
          type: item.type,
          name: item.name,
          x: item.x,
          y: item.y,
          width: item.width,
          height: item.height,
          rotation: item.rotation,
          status: item.status,
          note: item.note,
        })),
    })),
  }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run:
```bash
node --test --experimental-strip-types src/lib/venue/layout-sync.test.ts
```
Expected: PASS（3 个测试全通过）。

- [ ] **Step 5: 把测试加入 package.json 的 test 脚本**

把 `src/venue/layoutData.test.ts` 那一项后追加 ` src/lib/venue/layout-sync.test.ts`（同一行 `test` 脚本字符串末尾，空格分隔）。

- [ ] **Step 6: 跑全量测试确认未破坏其它用例**

Run:
```bash
npm test
```
Expected: 全部 PASS。

- [ ] **Step 7: 提交**

```bash
git add src/lib/venue/layout-sync.ts src/lib/venue/layout-sync.test.ts package.json
git commit -m "feat(venue): pure layout<->rows sync helpers with tests"
```

---

## Task 3：服务层 `service.ts`

**Files:**
- Create: `src/lib/venue/service.ts`

> 注：本服务直连 Supabase，不进 `node:test`（与 `src/lib/expenses/service.ts` 一样——它也没有单测）。验证靠类型检查 + Task 6 的端到端手测。

- [ ] **Step 1: 写实现**

`src/lib/venue/service.ts`：
```ts
import { createServerClient } from '@/lib/supabase/server'
import { layoutToRows, rowsToLayout } from '@/lib/venue/layout-sync'
import type { VenueRow, VenueFloorRow, VenueItemRow } from '@/lib/venue/layout-sync'
import type { VenueLayout } from '@/venue/layoutData'

export type ServiceErrorCode = 'invalid_input' | 'not_found' | 'forbidden' | 'db_error'

export interface ServiceError {
  code: ServiceErrorCode
  message: string
}

export type ServiceResult<T> =
  | { data: T; error: null }
  | { data: null; error: ServiceError }

const ok = <T,>(data: T): ServiceResult<T> => ({ data, error: null })
const err = <T = never,>(code: ServiceErrorCode, message: string): ServiceResult<T> =>
  ({ data: null, error: { code, message } })

export function httpStatusForError(code: ServiceErrorCode): number {
  switch (code) {
    case 'invalid_input': return 400
    case 'forbidden':     return 403
    case 'not_found':     return 404
    case 'db_error':      return 500
  }
}

// 单一共享场地的 id（迁移 029 预置）。
const SHARED_VENUE_ID = 'guild-main'

export async function getVenueLayout(): Promise<ServiceResult<VenueLayout>> {
  const db = createServerClient()

  const { data: venue, error: venueErr } = await db
    .from('venues')
    .select('id, name, width, height')
    .eq('id', SHARED_VENUE_ID)
    .single()
  if (venueErr) {
    if (venueErr.code === 'PGRST116') return err('not_found', 'venue not found')
    return err('db_error', venueErr.message)
  }

  const { data: floors, error: floorErr } = await db
    .from('venue_floors')
    .select('id, venue_id, name, width, height, background_image, sort_order')
    .eq('venue_id', SHARED_VENUE_ID)
  if (floorErr) return err('db_error', floorErr.message)

  const floorIds = (floors ?? []).map((f) => f.id)
  let items: VenueItemRow[] = []
  if (floorIds.length > 0) {
    const { data: itemRows, error: itemErr } = await db
      .from('venue_items')
      .select('id, floor_id, type, name, x, y, width, height, rotation, status, note, z_index')
      .in('floor_id', floorIds)
    if (itemErr) return err('db_error', itemErr.message)
    items = (itemRows ?? []) as VenueItemRow[]
  }

  return ok(rowsToLayout(venue as VenueRow, (floors ?? []) as VenueFloorRow[], items))
}

export async function saveVenueLayout(layout: VenueLayout): Promise<ServiceResult<VenueLayout>> {
  if (!layout || typeof layout.venueId !== 'string' || !Array.isArray(layout.floors)) {
    return err('invalid_input', 'invalid layout payload')
  }

  const db = createServerClient()
  const { venue, floors, items } = layoutToRows({ ...layout, venueId: SHARED_VENUE_ID })

  // 1) upsert 场地
  {
    const { error } = await db.from('venues').upsert(venue, { onConflict: 'id' })
    if (error) return err('db_error', error.message)
  }

  // 2) upsert 楼层
  if (floors.length > 0) {
    const { error } = await db.from('venue_floors').upsert(floors, { onConflict: 'id' })
    if (error) return err('db_error', error.message)
  }

  // 3) upsert 对象
  if (items.length > 0) {
    const { error } = await db.from('venue_items').upsert(items, { onConflict: 'id' })
    if (error) {
      // 23503 = 外键冲突：某对象被物品台账（Phase 1）以 RESTRICT 引用。
      if (error.code === '23503') {
        return err('invalid_input', '该场地对象仍被物品引用，无法修改/删除，请先在物品管理中改挂或删除。')
      }
      return err('db_error', error.message)
    }
  }

  // 4) 删除已移除的对象（按楼层范围，保留本次 payload 中的 id）
  const floorIds = floors.map((f) => f.id)
  const itemIds = items.map((i) => i.id)
  if (floorIds.length > 0) {
    let delItems = db.from('venue_items').delete().in('floor_id', floorIds)
    if (itemIds.length > 0) delItems = delItems.not('id', 'in', `(${itemIds.join(',')})`)
    const { error } = await delItems
    if (error) {
      if (error.code === '23503') {
        return err('invalid_input', '该场地对象仍被物品引用，无法删除，请先在物品管理中改挂或删除。')
      }
      return err('db_error', error.message)
    }
  }

  // 5) 删除已移除的楼层（cascade 会带走其对象）
  {
    let delFloors = db.from('venue_floors').delete().eq('venue_id', SHARED_VENUE_ID)
    if (floorIds.length > 0) delFloors = delFloors.not('id', 'in', `(${floorIds.join(',')})`)
    const { error } = await delFloors
    if (error) return err('db_error', error.message)
  }

  return getVenueLayout()
}
```

- [ ] **Step 2: 类型检查**

Run:
```bash
npx tsc --noEmit -p tsconfig.json
```
Expected: 无错误。

- [ ] **Step 3: 提交**

```bash
git add src/lib/venue/service.ts
git commit -m "feat(venue): service layer get/save venue layout"
```

---

## Task 4：API 路由 `GET/PUT /api/venue`

**Files:**
- Create: `src/app/api/venue/route.ts`

- [ ] **Step 1: 写实现**

`src/app/api/venue/route.ts`：
```ts
import { NextRequest, NextResponse } from 'next/server'
import { authGuard } from '@/lib/auth/guard'
import { getVenueLayout, saveVenueLayout, httpStatusForError } from '@/lib/venue/service'
import type { VenueLayout } from '@/venue/layoutData'

// GET /api/venue — 返回全团队共享布局
export async function GET() {
  const user = await authGuard()
  if (user instanceof NextResponse) return user

  const result = await getVenueLayout()
  if (result.error) {
    return NextResponse.json(
      { data: null, error: result.error.message },
      { status: httpStatusForError(result.error.code) },
    )
  }
  return NextResponse.json({ data: result.data, error: null })
}

// PUT /api/venue — 整份保存布局
export async function PUT(req: NextRequest) {
  const user = await authGuard()
  if (user instanceof NextResponse) return user

  let body: VenueLayout
  try {
    body = (await req.json()) as VenueLayout
  } catch {
    return NextResponse.json({ data: null, error: 'Invalid JSON body' }, { status: 400 })
  }

  const result = await saveVenueLayout(body)
  if (result.error) {
    return NextResponse.json(
      { data: null, error: result.error.message },
      { status: httpStatusForError(result.error.code) },
    )
  }
  return NextResponse.json({ data: result.data, error: null })
}
```

- [ ] **Step 2: 类型检查**

Run:
```bash
npx tsc --noEmit -p tsconfig.json
```
Expected: 无错误。

- [ ] **Step 3: 提交**

```bash
git add src/app/api/venue/route.ts
git commit -m "feat(venue): GET/PUT /api/venue route"
```

---

## Task 5：场地页改为读写 API + 一次性导入

**Files:**
- Modify: `src/app/[locale]/(app)/guild-venue/page.tsx`
- Modify: `messages/zh.json`、`messages/en.json`

> 现状参考：组件用 `parseStoredVenueLayout(localStorage...)` 在挂载时读取（约 96–102 行），并在另一个 effect 里 `writeStoredVenueLayout(...)` 每次变更写回（约 115–123 行）。本任务替换这两段。`SaveState = 'idle' | 'saved' | 'error'` 已存在并被 `StatusPill` 使用。

- [ ] **Step 1: 新增 i18n 文案**

在 `messages/zh.json` 的 `venue` 命名空间内新增：
```json
"loading": "加载场地中…",
"loadFailed": "场地加载失败，请刷新重试",
"importLocalPrompt": "检测到本浏览器存有旧的场地布局，是否导入到云端？",
"importLocal": "导入本地布局",
"importDone": "已导入",
"saveFailedToast": "保存失败：{message}"
```
在 `messages/en.json` 的 `venue` 命名空间内新增对应英文：
```json
"loading": "Loading venue…",
"loadFailed": "Failed to load venue. Please refresh.",
"importLocalPrompt": "Found an older venue layout in this browser. Import it to the cloud?",
"importLocal": "Import local layout",
"importDone": "Imported",
"saveFailedToast": "Save failed: {message}"
```

- [ ] **Step 2: 替换挂载加载逻辑（localStorage → API）**

把现有的挂载 effect（`parseStoredVenueLayout(window.localStorage.getItem(...))` 那段）替换为从 API 加载，并保留 localStorage 作为离线兜底。新增一个 `loading` 状态。

在 `useState` 区新增：
```tsx
const [loading, setLoading] = useState(true)
const [localImportLayout, setLocalImportLayout] = useState<VenueLayout | null>(null)
```

把原挂载 effect 替换为：
```tsx
useEffect(() => {
  let cancelled = false
  ;(async () => {
    try {
      const res = await fetch('/api/venue')
      const json = await res.json()
      if (cancelled) return
      if (!res.ok || !json?.data) throw new Error(json?.error ?? 'load failed')
      const layout = json.data as VenueLayout
      setHistory(createHistory(layout))
      setSelectedFloorId(layout.floors[0]?.id ?? DEFAULT_VENUE_LAYOUT.floors[0].id)
      setSelectedItemIds(layout.floors[0]?.items[0]?.id ? [layout.floors[0].items[0].id] : [])

      // 一次性导入：本浏览器存在自定义布局，且云端仍是默认种子时，提示导入。
      const localRaw = window.localStorage.getItem(VENUE_STORAGE_KEY)
      if (localRaw) {
        const local = parseStoredVenueLayout(localRaw)
        const cloudIsSeed = JSON.stringify(layout) === JSON.stringify(DEFAULT_VENUE_LAYOUT)
        const localDiffers = JSON.stringify(local) !== JSON.stringify(layout)
        if (cloudIsSeed && localDiffers) setLocalImportLayout(local)
      }
    } catch {
      if (cancelled) return
      // 离线兜底：用本地缓存或默认布局，标记保存失败。
      const fallback = parseStoredVenueLayout(window.localStorage.getItem(VENUE_STORAGE_KEY))
      setHistory(createHistory(fallback))
      setSelectedFloorId(fallback.floors[0]?.id ?? DEFAULT_VENUE_LAYOUT.floors[0].id)
      setSelectedItemIds(fallback.floors[0]?.items[0]?.id ? [fallback.floors[0].items[0].id] : [])
      setSaveState('error')
    } finally {
      if (!cancelled) {
        setLoading(false)
        setHydrated(true)
      }
    }
  })()
  return () => { cancelled = true }
}, [])
```

- [ ] **Step 3: 替换自动保存逻辑（localStorage → 防抖 PUT）**

把原先 `writeStoredVenueLayout(...)` 的 effect 替换为防抖保存到 API，同时仍写 localStorage 作离线缓存：
```tsx
useEffect(() => {
  if (!hydrated) return
  // 本地缓存兜底（即时）
  try { writeStoredVenueLayout(window.localStorage, layout) } catch { /* ignore quota */ }

  // 防抖 800ms 后保存到云端
  const handle = setTimeout(async () => {
    try {
      const res = await fetch('/api/venue', {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(layout),
      })
      const json = await res.json()
      if (!res.ok) throw new Error(json?.error ?? 'save failed')
      setSaveState('saved')
    } catch {
      setSaveState('error')
    }
  }, 800)
  return () => clearTimeout(handle)
}, [hydrated, layout])
```

- [ ] **Step 4: 渲染加载态与导入提示**

在 `if (!activeFloor) return null` 之前加入加载短路：
```tsx
if (loading) {
  return (
    <div className="h-[calc(100dvh-4rem)] flex items-center justify-center text-sm text-slate-500">
      {t('loading')}
    </div>
  )
}
```

在主返回的 `<Header ... />` 之后插入一次性导入条（仅当 `localImportLayout` 存在时）：
```tsx
{localImportLayout && (
  <div className="mb-3 rounded-lg border border-amber-200 bg-amber-50 px-4 py-2.5 flex items-center justify-between gap-3">
    <span className="text-sm text-amber-800">{t('importLocalPrompt')}</span>
    <button
      type="button"
      onClick={() => {
        commit(localImportLayout)
        setSelectedFloorId(localImportLayout.floors[0]?.id ?? selectedFloorId)
        setLocalImportLayout(null)
      }}
      className="shrink-0 h-8 rounded-lg bg-amber-600 px-3 text-xs font-semibold text-white hover:bg-amber-700"
    >
      {t('importLocal')}
    </button>
  </div>
)}
```
（点击后 `commit(localImportLayout)` 会把本地布局写入历史，进而触发 Step 3 的防抖保存到云端。）

- [ ] **Step 5: 类型检查**

Run:
```bash
npx tsc --noEmit -p tsconfig.json
```
Expected: 无错误。

- [ ] **Step 6: 端到端手测（见 Task 6）后提交**

```bash
git add "src/app/[locale]/(app)/guild-venue/page.tsx" messages/zh.json messages/en.json
git commit -m "feat(venue): load/save layout via API, one-time localStorage import"
```

---

## Task 6：端到端验证

**Files:** 无（仅验证）

- [ ] **Step 1: 启动并登录**

确认本地 dev server 运行（端口 3001），用已登录会话打开 `/zh/guild-venue`。

- [ ] **Step 2: 验证加载**

Expected: 页面先显示「加载场地中…」，随后渲染出预置的 1F（6 个对象）/ 2F（空）。

- [ ] **Step 3: 验证保存往返**

操作：拖动一个对象 → 等待约 1 秒 → 顶部状态变为「已保存」。刷新页面。
Expected: 刷新后对象停在新位置（数据来自数据库，不再依赖 localStorage）。

- [ ] **Step 4: 验证新增/删除楼层与对象往返**

操作：新增一个对象、删除一个对象、改名 → 等待保存 → 刷新。
Expected: 变更全部保留；被删除的对象不再出现。

- [ ] **Step 5: 验证离线兜底**

操作：临时停掉 dev server 或断网，拖动对象。
Expected: 顶部状态显示「失败」，但画布仍可编辑（本地缓存兜底），不崩溃。

- [ ] **Step 6: 验证一次性导入（可选场景）**

前置：在浏览器 localStorage 里保留一份与默认不同的旧布局、且数据库为默认种子。
Expected: 页面顶部出现「检测到本浏览器存有旧的场地布局…」提示；点「导入本地布局」后布局上云，刷新后保留，提示消失。

---

## Self-Review（作者自查记录）

- **Spec 覆盖**：本计划覆盖 spec「第 0 期」全部条目——三张表（Task 1）、`GET/PUT /api/venue`（Task 4）、整份 upsert + diff 删除（Task 3）、场地页改造 + 一次性导入 + 离线缓存（Task 5）、共享单场地（`SHARED_VENUE_ID`）。spec 中"删除被物品占用的区域被 RESTRICT 拦截"在本期为休眠路径（`items` 表尚不存在），服务层已预置 23503 错误映射，Phase 1 接入后即生效。
- **占位符扫描**：无 TODO/TBD；每个改代码的步骤均含完整代码。
- **类型一致性**：`VenueRow/VenueFloorRow/VenueItemRow` 在 `layout-sync.ts` 定义，被 `service.ts` 复用导入；`rowsToLayout/layoutToRows` 签名前后一致；`ServiceResult/httpStatusForError` 与 expenses 同形。venue_items 的 type/status 取值集合与 `layoutData.ts` 的 options 一致（迁移 Step 2 要求人工核对）。
- **范围**：本计划仅 Phase 0，独立可交付（场地持久化本身即有价值）。Phase 1（物品台账）、Phase 2（联动+照片）将各自成计划，依赖本期产出的 `venue_items` 表与 venue 模块。

## 后续计划（不在本文件）

- **Phase 1 计划**：`items` / `item_status_logs` / `item_code_counters` 迁移 030、`item_code` 触发器、`src/lib/items/*`、`/api/items`、`/items` 页面与表单、侧边栏新增物品管理、状态时间线。依赖本期 `venue_items`。
- **Phase 2 计划**：场地 inspector 联动展示该区域物品与总成本、`item-photos` 存储桶与照片上传。
