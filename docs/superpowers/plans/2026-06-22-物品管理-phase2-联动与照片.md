# 物品管理 Phase 2：场地联动 + 照片 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在场地画布选中某区域时展示放在该区域的物品及总成本；为物品加照片上传能力。

**Architecture:** 场地页额外拉取 `/api/items` 与 `/api/expenses`，把"放在当前选中区域"的物品汇总成轻量摘要传给 `VenueInspector` 渲染（区域物品列表 + 总成本）。照片走新建的公开 Storage 桶 `item-photos`：客户端把文件 POST 到 `/api/items/photo`，服务端用 service-role 上传并回传公开 URL，`ItemForm` 保存该 URL 到 `items.photo_url`。

**Tech Stack:** Next.js App Router、Supabase Storage（service-role 上传，公开读）、TypeScript、next-intl。

**前置依赖：** Phase 0（venue 持久化）、Phase 1（items 台账、`/api/items`、`ItemForm`、guild-venue 已接 API）必须已完成并合入。

---

## 文件结构

**新增：**
- `supabase/migrations/031_item_photos.sql` — 创建公开 Storage 桶 `item-photos`。
- `src/app/api/items/photo/route.ts` — `POST`：接收文件、service-role 上传、回传公开 URL。

**修改：**
- `src/venue/VenueInspector.tsx` — 新增可选 `placedItems` 区块（区域物品 + 总成本）。
- `src/app/[locale]/(app)/guild-venue/page.tsx` — 拉 items/expenses，计算选中区域的物品摘要并传入 inspector。
- `src/components/items/ItemForm.tsx` — 新增照片上传字段，写入 `photo_url`。
- `messages/zh.json` / `messages/en.json` — 联动与照片相关文案。

---

## Task 1：Storage 桶迁移 `031_item_photos.sql`

**Files:**
- Create: `supabase/migrations/031_item_photos.sql`

- [ ] **Step 1: 写迁移**

```sql
-- ============================================================
-- Migration 031: item photos storage bucket
-- 公开读取的物品照片桶；上传通过 service-role 后端路由完成。
-- ============================================================
insert into storage.buckets (id, name, public)
values ('item-photos', 'item-photos', true)
on conflict (id) do nothing;
```

- [ ] **Step 2: 应用并验证**

Run: `supabase db push`
Expected: `select id, public from storage.buckets where id='item-photos';` 返回一行且 `public = true`。

- [ ] **Step 3: 提交**

```bash
git add supabase/migrations/031_item_photos.sql
git commit -m "feat(items): public item-photos storage bucket (migration 031)"
```

---

## Task 2：照片上传路由 `POST /api/items/photo`

**Files:**
- Create: `src/app/api/items/photo/route.ts`

- [ ] **Step 1: 写实现**

`src/app/api/items/photo/route.ts`：
```ts
import { NextRequest, NextResponse } from 'next/server'
import { randomUUID } from 'node:crypto'
import { authGuard } from '@/lib/auth/guard'
import { createServerClient } from '@/lib/supabase/server'

const BUCKET = 'item-photos'
const MAX_BYTES = 5 * 1024 * 1024 // 5MB
const ALLOWED = new Set(['image/png', 'image/jpeg', 'image/webp', 'image/gif'])

export async function POST(req: NextRequest) {
  const user = await authGuard()
  if (user instanceof NextResponse) return user

  let form: FormData
  try {
    form = await req.formData()
  } catch {
    return NextResponse.json({ data: null, error: 'Invalid form data' }, { status: 400 })
  }

  const file = form.get('file')
  if (!(file instanceof File)) {
    return NextResponse.json({ data: null, error: 'file is required' }, { status: 400 })
  }
  if (!ALLOWED.has(file.type)) {
    return NextResponse.json({ data: null, error: '仅支持 PNG/JPEG/WebP/GIF 图片' }, { status: 400 })
  }
  if (file.size > MAX_BYTES) {
    return NextResponse.json({ data: null, error: '图片不能超过 5MB' }, { status: 400 })
  }

  const ext = file.name.includes('.') ? file.name.split('.').pop() : 'png'
  const path = `${randomUUID()}.${ext}`
  const buffer = Buffer.from(await file.arrayBuffer())

  const db = createServerClient()
  const { error } = await db.storage.from(BUCKET).upload(path, buffer, {
    contentType: file.type,
    upsert: false,
  })
  if (error) {
    return NextResponse.json({ data: null, error: error.message }, { status: 500 })
  }

  const { data } = db.storage.from(BUCKET).getPublicUrl(path)
  return NextResponse.json({ data: { url: data.publicUrl }, error: null }, { status: 201 })
}
```

- [ ] **Step 2: 类型检查并提交**

Run: `npx tsc --noEmit -p tsconfig.json` → 无错误。
```bash
git add src/app/api/items/photo/route.ts
git commit -m "feat(items): photo upload route (service-role -> item-photos)"
```

---

## Task 3：ItemForm 加照片上传字段

**Files:**
- Modify: `src/components/items/ItemForm.tsx`

> Phase 1 的 `ItemFormValue` 暂无 `photo_url`。本任务加上。

- [ ] **Step 1: 在 `ItemFormValue` 加字段并在 `toFormValue` 初始化**

`ItemFormValue` 接口加：
```ts
  photo_url: string | null
```
`toFormValue` 的返回对象加：
```ts
    photo_url: item?.photo_url ?? null,
```

- [ ] **Step 2: 加上传状态与处理函数**

在组件内（`const [saving, ...]` 附近）加：
```tsx
const [uploading, setUploading] = useState(false)

async function uploadPhoto(file: File) {
  setUploading(true)
  setError(null)
  try {
    const fd = new FormData()
    fd.append('file', file)
    const res = await fetch('/api/items/photo', { method: 'POST', body: fd })
    const json = await res.json()
    if (!res.ok) throw new Error(json?.error ?? 'upload failed')
    setValue((v) => ({ ...v, photo_url: json.data.url as string }))
  } catch (e) {
    setError(t('saveFailed', { message: e instanceof Error ? e.message : String(e) }))
  } finally {
    setUploading(false)
  }
}
```

- [ ] **Step 3: 在表单里渲染照片字段（放在备注字段之后）**

```tsx
<Field label={t('fieldPhoto')}>
  <div className="flex items-center gap-3">
    {value.photo_url && (
      // eslint-disable-next-line @next/next/no-img-element
      <img src={value.photo_url} alt="" className="w-14 h-14 rounded-lg object-cover border border-slate-200" />
    )}
    <input
      type="file"
      accept="image/png,image/jpeg,image/webp,image/gif"
      className="text-xs"
      onChange={(e) => { const f = e.target.files?.[0]; if (f) uploadPhoto(f) }}
    />
    {uploading && <span className="text-xs text-slate-400">{t('uploading')}</span>}
    {value.photo_url && (
      <button type="button" className="text-xs text-red-500 hover:underline" onClick={() => setValue((v) => ({ ...v, photo_url: null }))}>
        {t('removePhoto')}
      </button>
    )}
  </div>
</Field>
```

- [ ] **Step 4: 把 `photo_url` 纳入提交 payload**

在 `submit()` 的 `payload` 对象里加：
```ts
      photo_url: value.photo_url || null,
```

- [ ] **Step 5: 类型检查并提交**

Run: `npx tsc --noEmit -p tsconfig.json` → 无错误。
```bash
git add src/components/items/ItemForm.tsx
git commit -m "feat(items): photo upload field in item form"
```

---

## Task 4：场地 inspector 展示区域物品 + 总成本

**Files:**
- Modify: `src/venue/VenueInspector.tsx`
- Modify: `src/app/[locale]/(app)/guild-venue/page.tsx`

> 成本口径：单个物品成本贡献 = 其关联支出的 `unit_price × 物品 quantity`（仅实物且有成本时）。区域总成本 = 区域内各物品贡献之和。该口径避免多物品共享同一支出时按整单重复计。

- [ ] **Step 1: 给 VenueInspector 增加 placedItems 区块**

在 `VenueInspector.tsx` 顶部定义摘要类型并扩展 Props：
```ts
export type PlacedItemSummary = {
  id: string
  item_code: string
  name: string
  quantity: number
  cost: number
}
```
`Props` 加可选字段：
```ts
  placedItems?: PlacedItemSummary[]
  placedItemsTotalCost?: number
  onOpenItems?: () => void
```
函数签名解构加上 `placedItems = [], placedItemsTotalCost = 0, onOpenItems`。

在展开态（非 collapsed）的属性区块末尾、`onDelete` 按钮之前，插入区域物品区块：
```tsx
{item && (
  <div className="mt-4 rounded-lg border border-slate-200 p-3">
    <div className="flex items-center justify-between mb-2">
      <span className="text-xs font-semibold text-slate-700">{t('placedItemsTitle')}</span>
      {onOpenItems && (
        <button type="button" onClick={onOpenItems} className="text-xs text-indigo-600 hover:underline">
          {t('manageItems')}
        </button>
      )}
    </div>
    {placedItems.length === 0 ? (
      <div className="text-xs text-slate-400">{t('placedItemsEmpty')}</div>
    ) : (
      <>
        <ul className="space-y-1">
          {placedItems.map((p) => (
            <li key={p.id} className="flex items-center justify-between text-xs text-slate-600">
              <span className="truncate">{p.name} <span className="text-slate-400">×{p.quantity}</span></span>
              <span className="shrink-0 tabular-nums">¥{p.cost.toLocaleString('zh-CN')}</span>
            </li>
          ))}
        </ul>
        <div className="mt-2 flex items-center justify-between border-t border-dashed border-slate-200 pt-2 text-xs font-semibold text-slate-800">
          <span>{t('placedItemsTotal')}</span>
          <span className="tabular-nums">¥{placedItemsTotalCost.toLocaleString('zh-CN')}</span>
        </div>
      </>
    )}
  </div>
)}
```

- [ ] **Step 2: guild-venue 页拉取 items/expenses 并计算摘要**

在 `guild-venue/page.tsx` 加导入：
```tsx
import { useRouter } from '@/i18n/navigation'
import type { Item } from '@/lib/items/types'
import type { Expense } from '@/lib/types'
import type { PlacedItemSummary } from '@/venue/VenueInspector'
```
加状态与拉取（在已有 useState 区与 effect 区）：
```tsx
const router = useRouter()
const [items, setItems] = useState<Item[]>([])
const [expenseById, setExpenseById] = useState<Record<string, Expense>>({})

useEffect(() => {
  ;(async () => {
    try {
      const [itemsRes, exRes] = await Promise.all([fetch('/api/items'), fetch('/api/expenses')])
      const itemsJson = await itemsRes.json()
      const exJson = await exRes.json()
      if (itemsJson?.data) setItems(itemsJson.data as Item[])
      if (exJson?.data) {
        const map: Record<string, Expense> = {}
        for (const e of exJson.data as Expense[]) map[e.id] = e
        setExpenseById(map)
      }
    } catch { /* 联动展示是增强项，失败不阻断画布 */ }
  })()
}, [])
```
计算选中区域的物品摘要（`selectedItem` 在文件中已存在，是当前选中的 VenueItem | null）：
```tsx
const placedItems: PlacedItemSummary[] = useMemo(() => {
  if (!selectedItem) return []
  return items
    .filter((it) => it.placement_venue_item_id === selectedItem.id)
    .map((it) => {
      const ex = it.expense_id ? expenseById[it.expense_id] : null
      const cost = ex ? Number(ex.unit_price) * it.quantity : 0
      return { id: it.id, item_code: it.item_code, name: it.name, quantity: it.quantity, cost }
    })
}, [items, expenseById, selectedItem])
const placedItemsTotalCost = useMemo(
  () => placedItems.reduce((sum, p) => sum + p.cost, 0),
  [placedItems],
)
```
把这三个传给 `<VenueInspector ... />`：
```tsx
  placedItems={placedItems}
  placedItemsTotalCost={placedItemsTotalCost}
  onOpenItems={() => router.push('/items')}
```

- [ ] **Step 3: 类型检查**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无错误。（`useRouter` 来自项目的 `@/i18n/navigation`，与 Sidebar 用法一致。）

- [ ] **Step 4: 端到端手测（见 Task 6）后提交**

```bash
git add src/venue/VenueInspector.tsx "src/app/[locale]/(app)/guild-venue/page.tsx"
git commit -m "feat(venue): show placed items and total cost in zone inspector"
```

---

## Task 5：i18n 文案

**Files:**
- Modify: `messages/zh.json`、`messages/en.json`

- [ ] **Step 1: 在 `items` 命名空间补充照片相关键**

zh（`items` 内）：
```json
"fieldPhoto": "照片",
"uploading": "上传中…",
"removePhoto": "移除照片"
```
en（`items` 内）对应：`"fieldPhoto": "Photo"`、`"uploading": "Uploading…"`、`"removePhoto": "Remove photo"`。

- [ ] **Step 2: 在 `venue` 命名空间补充联动键**

zh（`venue` 内）：
```json
"placedItemsTitle": "该区域物品",
"placedItemsEmpty": "该区域暂无物品",
"placedItemsTotal": "总成本",
"manageItems": "去物品管理"
```
en（`venue` 内）对应：`"placedItemsTitle": "Items here"`、`"placedItemsEmpty": "No items in this zone"`、`"placedItemsTotal": "Total cost"`、`"manageItems": "Manage items"`。

- [ ] **Step 3: 提交**

```bash
git add messages/zh.json messages/en.json
git commit -m "feat(items): i18n for photo + venue placed-items surfacing"
```

---

## Task 6：端到端验证

**Files:** 无

- [ ] **Step 1: 照片上传**

`/zh/items` → 编辑/新增物品 → 选一张图片：出现「上传中…」后显示缩略图 → 保存。重新打开该物品，缩略图仍在（`photo_url` 已落库且公开可读）。
边界：上传 >5MB 或非图片类型 → 报错且不写入。

- [ ] **Step 2: 区域物品联动**

先在 `/items` 给某区域放 2 个实物（关联到不同支出）。打开 `/zh/guild-venue`，选中该区域 → 右侧 inspector 出现「该区域物品」列表（2 行，各显示 `名称 ×数量` 与成本），底部「总成本」= 两者 `unit_price×quantity` 之和。点「去物品管理」跳到 `/items`。

- [ ] **Step 3: 空区域**

选中一个没有物品的区域 → 显示「该区域暂无物品」。

- [ ] **Step 4: 全量测试与类型**

Run: `npm test` → PASS；`npx tsc --noEmit -p tsconfig.json` → 无错误。

---

## Self-Review（作者自查记录）

- **Spec 覆盖**：spec 第 2 期三项——区域物品 + 总成本（Task 4）、照片上传（Task 1/2/3）、可选软校验（spec 标注为 optional，本计划不实现，留待需要时再加）。区域物品数量角标 spec 未硬性要求，本计划以 inspector 列表代替，未做画布角标（可后续增量）。
- **占位符扫描**：无 TODO/TBD；改代码步骤均含完整代码或精确编辑位置。
- **类型一致性**：`PlacedItemSummary` 在 VenueInspector 定义并被 guild-venue 页导入复用；`Item`/`Expense` 类型来源与 Phase 1 一致；照片字段 `photo_url` 贯穿 ItemForm 值、payload 与 `items.photo_url` 列（Phase 1 已建列）。成本口径在 Task 4 注释中明确（`unit_price × quantity`）。
- **范围**：依赖 Phase 0/1 产出；本期为增强，失败路径（拉取 items/expenses 出错）不阻断场地画布主流程。

## 全功能收尾（三期合并后）

- 三期合并后跑一遍：`npm test`、`npx tsc --noEmit`、关键路径手测（场地持久化往返、物品增删改与状态日志、引用 RESTRICT、照片、区域联动）。
- 视情况更新 `src/lib/changelog/entries.ts`（用户可感知的新功能：物品管理 + 场地入库），符合版本管理员职责。
